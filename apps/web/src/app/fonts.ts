import { Inter, Jura } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

export const jura = Jura({
  subsets: ['latin'],
  variable: '--font-jura',
  display: 'swap',
})
